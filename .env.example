# Copy this file to .env and fill in your key. .env is git-ignored.
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override where per-run JSON traces are written (default: traces/).
# AGENT_TRACE_DIR=traces
